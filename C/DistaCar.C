#include <stdio.h>

int main()
{
    // e y anda 1km/2minutos  e y a cada hora se distancia 30km de x logo a distancia multiplicada por 2 sera o tempo que demorou para y chegar nela
    int km, tempo;
    scanf("%d", &km);
    tempo = km * 2;
    printf("%d minutos\n", tempo);
    return 0;
}